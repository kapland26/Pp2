SETUP:
  Npm install -g create-react-native-app
  create-react-native-app .
  //In app.json:
  {
    "expo": {
      "name": "myApp",
      "slug": "my-app",
      "sdkVersion": "18.0.0"
    }
  }
  npm start

Routing:
  npm i --save react-navigation